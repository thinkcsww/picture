import React from "react";
import { SafeAreaView, TextInput } from "react-native";

const SignUpClientScreen = () => {
  return (
    <SafeAreaView>
      <TextInput placeholder={"작가명을 입력해주세요."} />
    </SafeAreaView>
  );

};

export default SignUpClientScreen;
